<h1>Editing Project</h1>

<% form_for @project do |f| %>
  <p>
    <%= f.label :instructions %>
    <%= f.text_area :instructions, :cols => 120 %>
  </p>
  
  <p>
    <%= f.label :timeout, 'Build Timeout in Seconds' %>
    <%= f.text_field :timeout %>
  </p>
  <%= f.submit %>
<% end %>